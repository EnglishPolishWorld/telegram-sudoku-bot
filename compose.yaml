services:
  sudoku-bot:
    build: .
    restart: unless-stopped
    environment:
      BOT_TOKEN: ${BOT_TOKEN}
      DATABASE_PATH: /data/sudoku.sqlite3
    volumes:
      - sudoku-data:/data

volumes:
  sudoku-data:

